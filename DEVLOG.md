# NEMO-TNC 開発ログ

## 2026-06-04 — AFSK RX ループバックテスト方針と実装

### プロジェクト概要

ESP32-S3 上で動作する AX.25/FX.25 TNC（Terminal Node Controller）。  
Bell 202 AFSK（Mark=1200 Hz / Space=2200 Hz）で変調/復調し、USB CDC-ACM（KISS プロトコル）で PC と通信する。コールサイン: JH1FBM。

---

### 直前までの経緯（AFSK RX デバッグ）

直近 8 コミットで AFSK RX の安定化を繰り返してきた。主な問題と修正の流れ：

| コミット | 問題 | 修正 |
|---|---|---|
| `c0faea3` | envM=3308 だが ok=0 ng=0 | DC_ALPHA 0.005→0.05、BPF Q 2.5→1.0 |
| `347d59d` | プリアンブル 20 枚をほぼ全部見逃す | スケルチ時 s_ones を常にリセット、解除時 phase 初期化 |
| `3ef4b4e` | flags=6 ok=0（ABORT 繰り返し） | ビット判定を遷移検出より先に実行 |
| `2e0bc13` | envM≈envS で判別不能 | 19200 Hz 試行（→ WDT 発生） |
| `47ec973` | WDT（19200 Hz で adc_oneshot 間に合わず） | 9600 Hz 復帰、Q=2.5 復帰、判定点をビット末尾(sample7)に変更 |

**現在の確定パラメータ：**

| パラメータ | 値 | 理由 |
|---|---|---|
| サンプリングレート | 9600 Hz | 19200 Hz は WDT が発生 |
| BPF Q | 2.5 | Q=1 は 2200 Hz 5 次高調波が 1400 Hz にエイリアスして Mark BPF に混入 |
| ビット判定点 | sample 7（ビット末尾） | 群遅延 τ≈6.4 samp → 末尾で収束率 66% |
| DC_ALPHA | 0.05 | TX 開始時の DC 過渡を 2 フラグ以内に収束 |

---

### 現状の問題の根本原因分析

TX（`afsk_pwm_test.c`）は LEDC PWM で **矩形波** を出力している（50% デューティ）。  
矩形波は奇数倍の高調波を持つため：

- 2200 Hz の 5 次高調波 = **11000 Hz** → 9600 Hz サンプリングで **1400 Hz にエイリアス** → Mark BPF（中心 1200 Hz）に混入
- Q=2.5 で混入を 12.5% に抑えているが、それでも判別が不安定な可能性がある

さらに TX タイマー（833 µs/bit）と RX タイマー（104 µs/samp）が **同じ esp_timer タスク** を共有しており、同時動作時にタイミング干渉が起きる可能性もある。

---

### 検討した解決策の評価

#### アイデア 1：デジタル信号でループバック

**解釈(a)：ビット列を直接 `hdlc_process_bit()` に注入**

- HDLC/NRZI 層の確認には有効だが、問題の本丸（アナログフロントエンド・クロック回復）をスキップする
- 最近の修正はすべてその領域に集中しており、検証範囲が狭すぎる → **△**

**解釈(b)：計算済みサイン波サンプルを `afsk_sample_cb()` に直接注入**

- BPF → エンベロープ → クロック回復 → HDLC の **全経路** をクリーンな入力でテストできる
- ハードウェア変更なし、数十行で実装可能
- 「アルゴリズムに問題があるのか、信号品質（矩形波エイリアス）に問題があるのか」を切り分けられる → **◎ 採用**

#### アイデア 2：I2S モデムに切り替え

- `hw/i2s_device.c` は WIP（コンパイルエラーあり、DSP 処理未実装）
- 完成まで相当な工数が必要で、その間アルゴリズム検証ができない
- 長期的な方向性としては正しいが、**現時点では着手しない → ×**

#### 補足：ハードウェア LPF（今回は未実施）

PWM 出力ピン（GPIO17）と ADC 入力ピン（GPIO3）の間に RC LPF（R=1kΩ、C=47nF、fc≈3.4 kHz）を入れると、矩形波を近似サイン波に変換できる。アルゴリズム検証後に実施する。

---

### 今回の実装：サイン波ソフトウェアループバック

`main/modem/afsk_demod.c` に `AFSK_DEMOD_SINE_TEST` を追加。

#### 切り替え方法

```c
// この1行をコメントアウトするだけで実機 ADC に戻る
#define AFSK_DEMOD_SINE_TEST
```

#### 動作

1. 起動時に `test_signal_init()` が `ax25_build_ui_frame()` + `ax25_hdlc_frame()` で  
   "JH1FBM>APTEST: NEMO-TNC TEST" の完全な HDLC ビット列を生成（プリアンブル 20 フラグ込み）
2. 9600 Hz タイマーコールバックごとにサイン波サンプルを生成して ADC の代わりに入力
3. **無音 2 秒 → フレーム送出 → 無音 2 秒** を繰り返し（スケルチ動作も確認できる）

#### 期待されるログ

```
I AFSK_RX: SINE TEST MODE: ADC をスキップし内部サイン波を入力
I AFSK_RX: SINE TEST: UI 31 bytes -> HDLC 71 bytes / 568 bits
（2秒後）
I AFSK_RX: FLAG sync (DC=2048 envM=... envS=...)
I AFSK_RX: RX OK 31 bytes (ok=1 ng=0)
```

#### 結果の読み方

| 結果 | 意味 | 次のアクション |
|---|---|---|
| RX OK が出る | アルゴリズム正常 | 実 ADC テストへ。問題は矩形波エイリアス |
| flags は出るが RX OK が出ない | クロック回復に問題 | ビットタイミングのさらなる調整 |
| FCS NG しか出ない | HDLC/NRZI に問題 | フレームバイト列を直接デバッグ |
| flags すら出ない | BPF・エンベロープ・スケルチに問題 | 定期ログで envM/envS の値を確認 |

---

### 今後のロードマップ

1. **Step 1（本日実施）** — サイン波ソフトウェアループバックでアルゴリズム検証
2. **Step 2** — ハードウェア LPF（RC 1 段: R=1kΩ, C=0.047μF(473)）を追加して実 PWM 信号でテスト
3. **Step 3（長期）** — V4220M コーデック + I2S 実装（`hw/i2s_device.c` の完成）

---

## 2026-07-08 — サイン波ループバック RX OK 達成（ビットクロック復元の2バグ修正）

### 経緯

Step 1 のサイン波ループバックテストを実施したところ、プリアンブルの FLAG sync は出るが RX OK / FCS NG が一切出ない状態だった。`hdlc_process_bit()` のアボート分岐と、フラグ確定時の valid_bits 不一致分岐に診断ログ (`ABORT` / `FRAME DROP`) を追加して原因を切り分けた。

ハードウェアでの試行錯誤は遅いため、`afsk_demod.c` のアルゴリズム（BPF 係数・エンベロープ・ヒステリシス・ビットクロック復元・HDLC 状態機械）をそのまま Python に移植し、同一のサイン波テスト信号を与えてホスト上で高速に再現・検証した。

### 発見した2つのバグ

**バグ①: resync とビット判定の競合（二重判定）**

`s_phase == DEMOD_SPB-1` でビット判定した直後、同じサンプルでトーン遷移が検出されると resync 処理が `s_phase = 6` に上書きし、直後の位相カウンタ更新で `s_phase = 7` に戻ってしまう。その結果、**次のサンプルで即座にもう一度ビット判定**が発生し、1ビットが2ビットとして重複カウントされていた。

**バグ②: 判定直後の BPF リンギング誤検出**

バグ①を直すと、今度はフラグの最後 (7連続1のあとの0) を取りこぼしてアボートするケースが規則的に発生した。原因は BPF の群遅延がちょうど整数値ではない (τ≈6.4 samp) ため、本物のトーン遷移直後に BPF 出力がわずかにリンギング（1〜2 サンプルだけトーンが跳ね返る）し、これを resync が「新しい遷移」と誤認して早すぎるタイミングで再同期・ビット判定してしまうこと。

### 修正

- ビット判定タイミングを `phase == DEMOD_SPB-1`（ビット末尾）から `phase == 0`（ビット境界の1サンプル後）に変更
- ビット判定と同じサンプルで遷移が検出された場合は resync をスキップ（バグ①対策）
- **resync 発火後だけでなく、通常のビット判定の直後にも** `s_sync_lockout` をセットしてリンギングによる誤 resync を防止（バグ②対策）

### 検証方法

`afsk_demod.c` のロジックを Python に移植したシミュレータで、サイン波テスト信号を 15〜21 バースト連続再生。修正前は全バーストで RX OK 0 件だったが、修正後は **21/21 バーストで RX OK（FCS OK）、FCS NG は 0 件**。実機でも同じ修正を適用したところ、`RX OK 31 bytes (ok=4,5,6... ng=0)` が安定して連続成功することを確認した。

### 次のアクション

Step 2（ハードウェア LPF を追加して実 PWM 信号でテスト）に進める状態になった。

---

## 2026-07-09 — ハードウェア LPF 経由の実信号テスト（Step 2 実施・限界を確認）

### 回路構成

ブレッドボードに RC 1 段 LPF を組んで TX (GPIO17 PWM) → RX (GPIO3 ADC) をアナログループバック接続。

```
GPIO17 ── R(680Ω) ──┬──► GPIO3 (ADC1_CH2)
                     │
                   C(0.068μF, 683)
                     │
                    GND
```

fc ≈ 3.4kHz（設計値 R=1kΩ/C=0.047μF(473) とほぼ同一特性）。PWM は 50% デューティなので DC バイアス回路は不要。

### 問題①: GPIO17 に電圧が出ない

テスターで GPIO17-GND 間を計測しても電圧が出なかった。原因はハード配線ではなく **TNC が何も送信していなかった** こと。現行コードは KISS/USB 経由で実パケットを受信した時のみ PWM を出す設計になっており、サイン波ソフトウェアループバック（`AFSK_DEMOD_SINE_TEST`）はデモジュレータ内で完結する仮想信号でTX経路を一切通らないため、`afsk_pwm_test.h` のドキュストリングが謳っていた「起動3秒後から5秒間隔の自動テスト送信」は実装から失われていた。

**対応**: `afsk_pwm_test.c` に `AFSK_PWM_SELFTEST` を追加。起動 3 秒後から 5 秒間隔でテスト AX.25 UI フレーム (`JH1FBM>APTEST`) を `afsk_tx_buf` へ自動投入し、KISS クライアントなしでベンチテストできるようにした（本番運用時はマクロをコメントアウトして無効化する）。

### 問題②: 実信号での FLAG sync は出るが安定してデコードできない

`AFSK_DEMOD_SINE_TEST` を無効化し実 ADC 入力に切り替えてテスト。TX 自体は正常に動作 (`TX port0 JH1FBM>APTEST ... HDLC=53 bytes / 424 bits`)、RX も FLAG sync を検出するが、1 回の送信 (424 bit ≈ 353ms) の間に FLAG sync / ABORT が何度も連続発生し、安定した RX OK に至らない。

envM/envS の分離が弱い（例: envM=1703.3 vs envS=1193.3、比率 1.43 倍）。ソフトウェアサイン波テストでは同様の場面で比率 4.7 倍程度あった。

**原因分析**: `afsk_pwm_test` の出力は矩形波（LEDC PWM を音声周波数そのもので駆動）であり、奇数次高調波を強く含む。現在の RC LPF は 1 段 (-6dB/oct) の緩やかな減衰特性で、しかも Mark(1200Hz)/Space(2200Hz) 自体がカットオフ (3.4kHz) に近いため、高調波除去が不十分なまま ADC に入力されている。これは DEVLOG 2026-06-04 時点で既に懸念されていた内容（「Q=2.5 でも混入を 12.5% に抑えているが、それでも判別が不安定な可能性がある」）が実測で裏付けられた形。

### 方針決定

RC フィルタを多段化する延命策よりも、**Step 3（V4220M コーデック + I2S 実装）に進み、PWM 矩形波ではなく本物のサイン波を送信する方向へ切り替える**。`afsk_pwm_test` は暫定実装であり、根本解決には音声帯域の適切な D/A 出力が必要という判断。

### 次のアクション

Step 3（`hw/i2s_device.c` の完成、V4220M コーデックによる I2S 送受信）に着手する。

---

## 2026-07-11 — V4220M + I2S ブリングアップ実装（動作確認前）

### 配線

```
IO47 -> RSTN  (コーデックリセット, active low)
IO42 -> XTI   (コーデック外部クロック入力 = ESP32 I2S MCLK)
IO41 -> LRCK  (ワードセレクト, ESP32 -> コーデック)
IO40 -> SCLK  (ビットクロック, ESP32 -> コーデック)
IO39 -> DOUT  (コーデックのデータ出力 -> ESP32 I2S din, RX方向)
IO38 -> DIN   (コーデックのデータ入力 <- ESP32 I2S dout, TX方向)
```

ESP32-S3 を I2S マスター、V4220M をスレーブとする構成。

### V4220M データシート調査

Web検索とPDF抽出（`akizukidenshi.com/goodsaffix/v4220m.pdf` 等）で以下を確認：

- DIF1/DIF0 ピン (00=I2S) でフォーマット選択。I2C/SPI制御ポートと共用ピンだが、今回の配線に SDA/SCL/CS が無いことから、ボード側で DIF0/DIF1 を GND 固定しハードウェアモード（レジスタ制御なし）で動作させる設計と判断。
- **マスター/スレーブ判定**: 「DOUT ピンに 47kΩ プルダウン抵抗が起動時/リセット後に存在すると V4220M 自身がマスターモードで起動する」。今回は ESP32 マスター前提のため、この抵抗が実装されていないことが必須。ユーザーに確認したところ、基板上はフットパターンのみで未実装 → 問題なし。
- Slave モードでの規定値: `XTI=256/384/512×Fs`, `LRCK=4〜50kHz`, `SCLK=48/64/128×Fs`。

### 実装

`main/hw/i2s_device.c` / `i2s_device.h` を全面書き直し。旧WIPコードは複数のコンパイルエラー（`static`変数の二重定義、未定義関数呼び出し、構造体の前方参照、余分な`app_main`等）を抱えたまま `CMakeLists.txt` のビルド対象から外されていた状態だった。`main/CMakeLists.txt` に `hw/i2s_device.c` と `esp_driver_i2s` コンポーネントを追加し、ビルド対象に組み込んだ。

**サンプリングレート**: 48000Hz を採用（ユーザー判断）。旧 ADC oneshot 方式の 9600Hz は `adc_oneshot_read()` の実行時間による制約だったが、I2S/DMA 化によりこの制約はなくなるため、標準的なオーディオレートに変更。ただし **`afsk_demod.c` 側の BPF係数・DEMOD_SPB（48kHz では 40 サンプル/ビット）・ビットタイミングは 9600Hz 前提のまま未対応** — 別タスクとして残っている。

**24bit スロット幅のバグ**: ESP-IDF公式ドキュメント (`i2s.rst`) を確認したところ、ESP32-S3 では `slot_bit_width` を AUTO（=24bit）のままにすると DMA バッファが 3byte 詰めになり、`int32_t left/right` の構造体と噛み合わなくなることが判明（この挙動はチップにより異なり、無印ESP32では32bit詰めになる）。`slot_bit_width` を明示的に `I2S_SLOT_BIT_WIDTH_32BIT` に設定することで回避。これにより BCLK=64×Fs となり、V4220M のスレーブモード規定値とも一致する。サンプル値は各32bitスロットの上位24bitにMSB詰め（`<<8` / `>>8`）で格納する。

### アナログループバックテスト

外部入力回路（マイク/ライン入力）がまだ無いため、V4220M の DAC 出力 (LOUT/ROUT) を ADC 入力 (LIN/RIN) にジャンパ線で直結し、アナログ経由で疎通確認するテストを実装（`i2s_device_start_analog_loopback_test()`）。

- Mark(1200Hz)/Space(2200Hz) のサイン波を2秒おきに切り替えて左右両チャンネルに送信（どちらのチャンネルをジャンパしても検出できるように、左右に同じ信号を出力）
- 受信側は左右チャンネルを独立に解析し、ピーク振幅とゼロクロス数からの推定周波数を1秒ごとにログ出力
- `tnc_init.c` から起動時に自動実行されるようにした

### 現状

**ビルドは成功、実機での動作確認はこれから。** 次回、LOUT/ROUT→LIN/RIN のジャンパを接続した状態で flash し、ループバックログ（ピーク振幅・推定周波数）を確認する。

### 次のアクション

1. 実機でアナログループバックテストの結果を確認
2. 問題なければ、`afsk_demod.c` の AFSK 復調ロジックを ADC oneshot から I2S DMA 経由に置き換え、48kHz 用に BPF係数・DEMOD_SPB・ビットタイミングを再設計する
3. `afsk_pwm_test.c`（LEDC PWM矩形波送信）も I2S 経由のサイン波送信に置き換える

### 使用ボード / 物理ピン位置の確認

ボードは **ESP32-S3-DevKitC-1-N8R8**（v1.1）。GPIO47/42/41/40/39/48 は全て **J3ヘッダー**上にあり、Espressif公式ユーザーガイドで物理位置を確認した。

| J3ピン番号 | GPIO | 用途 |
|---|---|---|
| 6 | GPIO42 | XTI |
| 7 | GPIO41 | LRCK |
| 8 | GPIO40 | SCLK |
| 9 | GPIO39 | DOUT |
| 16 | GPIO48 | DIN |
| 17 | GPIO47 | RSTN |

GPIO42/41/40/39 は連番（6〜9番ピン）、GPIO47はそこから7つ離れた17番ピン、GPIO48は16番ピン。

**DIN ピンの変更**: 当初 GPIO38 を予定していたが、ESP32-S3-DevKitC-1 **v1.1 ではGPIO38がオンボードRGB LED (WS2812) と共用**であることが判明（v1.0ではGPIO48がLED用だったが、v1.1でGPIO38に変更されたという公式ドキュメントの記載を確認）。信号品質への影響を避けるため、DINをv1.1ボードで空きピンとなっているGPIO48に変更した（J3ヘッダー16番ピン）。GPIO48はOctal PSRAM用の差動クロック副機能を持つが、v1.0でLED駆動という一般GPIO用途に使われていた実績があり、通常のGPIOとして問題なく使用できる。

なお GPIO35〜37 (J3ヘッダー11〜13番ピン) は N8R8 (Octal PSRAM) 構成では内部フラッシュ/PSRAM用に予約されており外部使用不可。

---

## 2026-07-23 — アナログループバック不調の切り分けと根本原因判明（クロック方式の設計ミス）

### 経緯

ESP32-S3マスター/V4220Mスレーブ構成でアナログループバックテストを実施したところ、送信トーン(Mark/Space)に関係なく **受信側が常に24bitフルスケール付近に張り付き、推定周波数が常に Fs/4 (≒12000Hz) 固定** という壊れた値になる問題が発生。以下の手順で切り分けた。

1. **配線確認**: DIN/DOUT・DIF0/DIF1/DEM0/DEM1のGND接続・LOUT/LIN差動配線・RSTN配線・電源電圧、いずれもハードウェア上は問題なし（5Vのみ4.66Vとやや低いが、決定打ではないと判断）
2. **I2S read/writeをバッファ化**: 1フレーム(8byte)ずつの転送をやめ256フレーム単位のバッチ転送に変更したが症状変わらず → フレーム境界のズレが原因ではないと判明
3. **決定的な切り分け**: V4220Mを経由せず、**ESP32のGPIO39-GPIO48を直接ジャンパして自己ループバック**したところ、peak・freqとも送信内容と正しく一致 → **ESP32側のI2S設定・ソフトウェアは正常**と確定。バグはV4220M側にあると判明

### 根本原因: ESP32からのMCLK供給によるXTI駆動が機能しない

同じ構成（ESP32-S3 + V4220M）で自作した先行事例（[b.meso.tokyo の記事](https://b.meso.tokyo/post/819422500441243649/create-digital-audio-effector-with-esp32s3-and-v4220m)）を発見。著者も「ESP32のI2Sマスタークロック出力をXTIに供給する方式は、どうやっても正常に動作させることができなかった」と明記しており、今回の症状と完全に一致する。

**原因の推定**: XTIは本来水晶発振子を跨いで接続するアナログ発振回路のピンであり、ESP32のGPIOから汎用I2S周辺機能経由で出力する単純なデジタル方形波では、V4220M内部のΔΣ変調器が正しくクロックロックできない（ジッタ・信号品質・バイアスの問題と推定）。

**先行事例の解決策**:
- XTI/XTO間に **12.288MHz (256×48kHz) の外部水晶発振子** を直結し、V4220M自身にマスタークロックを自己生成させる
- **V4220Mを I2Sマスター**（SCLK/LRCKを出力）、**ESP32をI2Sスレーブ**（受信）に変更
- DOUTピンに **47kΩプルダウン抵抗を実装**（V4220Mを自身のマスターモードに入れるため。以前「未実装が正しい」としていたのと逆）

この変更はハードウェア追加（水晶発振子・プルダウン抵抗）とソフトウェア変更（ESP32 I2S役割をマスター→スレーブに変更、MCLK出力を廃止）の両方が必要な大きめの対応のため、方針検討中。

### 副次的な発見: DEM0/DEM1 の設定誤り

V4220Mは Cirrus Logic **CS4220/CS4221系列と互換**と考えられる。CS4220データシート **Table 5 (De-Emphasis filter control)** で確認したところ：

| DEM1 | DEM0 | De-emphasis |
|---|---|---|
| 0 | 0 | 32 kHz |
| 0 | 1 | 44.1 kHz |
| 1 | 0 | 48 kHz |
| **1** | **1** | **OFF** |

以前「DEM0・DEM1はGNDに接続」と案内していたのは誤りで、**GND(0,0)にすると32kHzのディエンファシスフィルタが有効になってしまう**。正しくは **DEM0=1, DEM1=1 (両方とも3.3V/VLへ)** でディエンファシス無効となる。前掲のb.meso.tokyo記事とも一致しており、これもループバック不調の一因だった可能性がある。クロック方式の変更を待たずに先に修正する価値がある。

### ソフトウェア側の変更（今回コミット分）

- I2Sアナログループバックテストのread/writeを1フレーム単位からバッファ単位(256フレーム)に変更（フレーム境界ズレの可能性を排除する目的。今回は原因ではなかったが、ESP-IDFの標準的な使い方として妥当なため残す）
- GPIO38のオンボードLED誤点灯対策として`indicator_turn_off_onboard_led()`を追加したが、外付けLED(GPIO4)をGPIO38へ一時的に切り替えて検証した後、GPIO4に戻した（現状は関数のみ残し呼び出しはコメントアウト/削除）

### 次のアクション

1. クロック方式変更（外部水晶 + V4220Mマスター化 + ESP32スレーブ化）の対応方針を検討
2. DEM0/DEM1を3.3V側に配線し直す（クロック方式と独立に先行して直せる）
3. 上記ハードウェア変更後、ソフトウェアのI2S役割をスレーブに書き換える
4. （参考）ネイティブUSBの2ポートCDCとは別に、書き込み・コンソール用の独立経路としてCH340C/CH340K等（DTR#/RTS#両方使える機種）をUART0(GPIO43/44)に接続する構成を検討中
